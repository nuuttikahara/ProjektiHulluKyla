<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HulluKyla.Pages.TestiSivu"
             Title="TestiSivu">
    <VerticalStackLayout>
        <CollectionView x:Name="AsiakasLista" SelectionMode="Single" SelectionChanged="AsiakasSelected" HeightRequest="200">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="2">
                        <Label Text="{Binding etunimi}" FontSize="14" FontAttributes="Bold"/>
                        <Label Text="{Binding sukunimi}" FontSize="14" FontAttributes="Bold"/>
                        <Label Text="{Binding email}" FontSize="14"/>
                        <Label Text="{Binding puhelinnro}" FontSize="14"/>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>